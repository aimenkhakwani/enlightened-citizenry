import { chamberName } from 'enlightened-citizenry/helpers/chamber-name';
import { module, test } from 'qunit';

module('Unit | Helper | chamber name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = chamberName([42]);
  assert.ok(result);
});
