import { enactedBoolean } from 'enlightened-citizenry/helpers/enacted-boolean';
import { module, test } from 'qunit';

module('Unit | Helper | enacted boolean');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = enactedBoolean([42]);
  assert.ok(result);
});
